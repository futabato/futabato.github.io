---
interface Props {
  year: string;
  items: string[];
}

const { year, items } = Astro.props;
---

<div class="timeline-item">
  <div class="timeline-item__year">{year}</div>
  <ul class="timeline-item__content">
    {items.map((item) => (
      <li>{item}</li>
    ))}
  </ul>
</div>
